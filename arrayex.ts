let userarr : string[] = ['John', 'Jane', 'Doe'];

console.log(userarr.length)

userarr.push('Smith');
console.log(userarr);

userarr.pop();

console.log(userarr);

let scores: Array<number> = [90, 80, 70, 60];
let score:number[] = [100, 95, 85, 75];
console.log(scores.length);
